# basics
wheel>=0.30.0
pip>=9.0.1

# Project management and Deployment
bumpversion>=0.5.3
watchdog>=0.8.3
twine>=1.9.1
# virtualenvwrapper>=4.8 deprecated; use Makefile targets and CI matrix
# virtualenv>=16.0.0 deprecated; use Makefile targets and CI matrix

# Testing
# tox deprecated; use Makefile targets and CI matrix instead
flake8>=3.6.0
coverage>=4.4.1
pytest>=8.4
pylint>=1.7.4
deepdiff>=3.3.0

# Docs
Sphinx>=1.6.3
sphinx_rtd_theme>=0.2.5b1

# Code Deps (none required beyond runtime requirements for dev)
